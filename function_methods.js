function getAsignment(asignment1, asignment2, asignment3) {
    const total = asignment1 + asignment2 + asignment3
    const average = total / 3
    return average
}
const result=getAsignment(50,55,60)
console.log(result)